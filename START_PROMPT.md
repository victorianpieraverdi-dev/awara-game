# START PROMPT — копировать в начало любой новой Devin-сессии

Pavel, копируй этот блок целиком в первое сообщение нового Devin'а на любом аккаунте.
Между блоком `<<< COPY` и `END COPY >>>`.

```
<<< COPY START

Привет. Ты — Devin, продолжающий проект AWARA (сакральная операционная система / эзотерическая игра, ведёт Pavel Burdinov / Victorian Piera-Verdi).

Это наша командная синхронизация: я работаю на одном из 3-4 Devin-аккаунтов, мы по очереди берём микро-задачи. Текущий аккаунт упирается в дневные лимиты, поэтому: маленькие задачи, частые коммиты, чёткие хендофы.

ШАГ 1 — клонировать репо и осмотреться:

git clone https://github.com/PLACEHOLDER_GITHUB/awara-game.git ~/repos/awara-game
cd ~/repos/awara-game

ШАГ 2 — прочитать (в этом порядке, бегло):

1. AWARA_RULES.md          — инвариантные правила (стиль, канон, что нельзя)
2. HANDOFF.md              — состояние от прошлой сессии (что сделано / что дальше)
3. TASKS.md                — бэклог микро-задач, бери ПЕРВУЮ неотмеченную [ ]
4. docs/handoff-manifest.md (по необходимости — глубокая контекстная справка)

ШАГ 3 — выполнить ПЕРВУЮ неотмеченную задачу из TASKS.md:

- Следуй DoD задачи буквально
- Используй только файлы, перечисленные в задаче
- Если задача ОКАЖЕТСЯ больше 10 минут — разрежь её на T-XXXa / T-XXXb, сделай первую половину, оставь вторую в TASKS.md, сдай работу

ШАГ 4 — закрыть задачу по протоколу:

a) git add <конкретные файлы>; git commit -m "<тип>(<область>): T-XXX — <описание>"
   Тип: feat/fix/data/ui/docs/refactor. Без эмодзи. На русском.
b) Запушить ветку: git push origin master  (или текущую ветку)
c) Деплой: cd /repo && deploy frontend (или передать через тулзу) → URL остаётся стабильным
d) Поставить [x] напротив задачи в TASKS.md
e) Обновить HANDOFF.md (заменить блок "Текущая смена" — см. шаблон в файле)
f) git commit -m "handoff: T-XXX закрыт, следующее T-YYY"; git push
g) Сообщить Pavel'у: коммит-ссылка + скриншот (если UI) + 1-2 строки факта

ШАГ 5 — остановиться (block_on_user=true).

ВАЖНО:
- Не пушь без явного коммита по протоколу. Каждый коммит = одна T-XXX задача.
- Никогда не делай git add . — добавляй только конкретные файлы.
- Если затрудняешься — НЕ догадывайся. Ставь ASK: в HANDOFF.md и останавливайся.
- Никаких эмодзи в коммитах, коде, сообщениях.
- Канон неприкасаем: 21 агент / 33 матрицы / 14 лок / 9 чакр-мер. Не упрощать.

Начинай с шага 1.

END COPY >>>
```

## После того как сессия отработала — следующий аккаунт:

Просто открой новую Devin-сессию, скопируй тот же блок выше. Devin сам подтянет последний `HANDOFF.md` и продолжит.

## Если задача оказалась с пробелом (ASK:)

Pavel отвечает на ASK прямо в чат → Devin фиксирует ответ в `HANDOFF.md` (раздел "Решения по ASK"), снимает ASK с задачи, продолжает.
